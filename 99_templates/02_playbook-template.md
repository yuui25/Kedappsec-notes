<<<BEGIN>>>
# 02_playbook-template.md（最小手順・分岐中心の雛形）

> 用途：`02_playbooks/` 配下の各ファイルを、この雛形で作成する。  
> 方針：playbookは「手が止まらないための最小手順」。topicsの深掘りを前提に、分岐と判断基準を中心に書く。  
> 注意：本文内でコードやコマンド例を書く場合は `~~~~` 〜 `~~~~` を使用する（``` は使わない）。  
> 禁止：網羅的チェックリスト化、経験に基づく「落とし穴」章、報告テンプレの肥大化。

---

## タイトル
（例：Web Recon 入口→境界→検証方針 / 認可 境界モデル→検証観点チェック など）

## 目的（このプレイブックで到達する状態）
- （例）短時間で攻め筋の優先度を付け、次に読むtopic/回す検証を確定できる
- （例）判断が必要な分岐で迷わずに次の一手を選べる

## 前提（対象・範囲・制約）
- 対象（Web/NW/ASM/SaaS）：
- 制約（時間・権限・到達性・環境）：
- 前提ツール/環境（最小限）：
- 参照すべきtopics（最初に読む/途中で参照）：

## 入口で確定すること（最小セット）
- 対象の境界（資産/信頼/権限）をどう把握するか：
- 何が分かれば「次の段階に進める」か（完了条件）：

## 手順（分岐中心：迷うポイントだけ）
> ここは「順番」より「判断」。結果によって次が変わる形で書く。

### Step 1：最初に観測する（入口）
- 目的：
- 観測ポイント：
- 証跡（最小）：
- 次の分岐：
  - もしAなら → Step 2Aへ
  - もしBなら → Step 2Bへ

### Step 2A：仮説Aを検証する
- 目的：
- 観測ポイント：
- 次の分岐：
  - もしA-1なら → Step 3Aへ
  - もしA-2なら → Step 3Bへ

### Step 2B：仮説Bを検証する
- 目的：
- 観測ポイント：
- 次の分岐：
  - もしB-1なら → Step 3Cへ
  - もしB-2なら → Step 3Dへ

### Step 3：攻め筋の確定（優先度付け）
- 優先度の付け方（判断基準）：
- 次に深掘りするtopics（最大3つ）：
- 次に回す検証（labs連動）：

## 取得する証跡（目的ベースで最小限）
- 何のために取るか（再現性ではなく技術検証の観測として）：
- 取得対象（例：har/pcap/ログ/クラウド監査ログ等）：
- 見るポイント（差分・境界・権限伝播・到達性）：

## コマンド/リクエスト例（例示は最小限）
~~~~
（必要最小限の例：コマンド/リクエスト/設定例）
~~~~
- 何を観測する例か：
- 出力の注目点：
- 前提が崩れるケース：

## ガイドライン対応（ASVS / WSTG / PTES / MITRE ATT&CK：毎回記載）
- ASVS：
  - 該当領域/章：
  - 該当要件（可能ならID）：
  - このプレイブックが支える管理策（観測/検証/判断のどこか）：
- WSTG：
  - 該当カテゴリ/テスト観点：
  - 該当が薄い場合：このプレイブックが支える前提（情報収集/境界特定 等）：
- PTES：
  - 該当フェーズ：
  - 前後フェーズとの繋がり（1行）：
- MITRE ATT&CK：
  - 該当戦術（必要なら技術）：
  - このプレイブックが支える攻撃者の意図（例：Discovery、Credential Access等）：

## 報告（ガイドライン程度：数行で）
- 事実（観測結果）：
- 成立条件（前提・条件差）：
- 影響（推定は推定と明記）：
- 対策方向性（最小限）：

## リポジトリ内リンク（最大3つまで）
- 関連 topics：
- 関連 labs：
- 関連 cases：

---

## 自己チェック（送信前に必ず確認）
- 網羅チェックリスト化していない（分岐と判断が主）
- “落とし穴”章がない
- 報告が肥大化していない（ガイドライン程度）
- ガイドライン4種（ASVS/WSTG/PTES/ATT&CK）が毎回書かれている
- 内側コードが `~~~~` で、``` を使っていない

<<<END>>>